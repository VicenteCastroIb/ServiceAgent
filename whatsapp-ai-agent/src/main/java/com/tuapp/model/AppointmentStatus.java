package com.tuapp.model;

/** Estado de una cita agendada (ver Appointment y SchedulingService). */
public enum AppointmentStatus {
    CONFIRMADA,
    CANCELADA,
    REAGENDADA,
    COMPLETADA,
    NO_SHOW
}
